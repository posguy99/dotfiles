#
# proctools
#

function pkill {
    local pid
    pid=$(ps ax | grep $1 | grep -v grep | awk '{ print $1 }')
    kill -9 $pid
    echo -n "Killed $1 (process $pid)"
}

function pgrep {
    ps aux | grep $1 | grep -v grep
}

